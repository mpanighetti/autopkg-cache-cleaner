# AutoPkg Cache Cleaner

This script searches for and deletes all downloaded resources and compiled packages in an AutoPkg recipe cache with creation dates older than the specified number of days.

The script expects to run in user context (the same user account running AutoPkg) and does not require `sudo` for a typical AutoPkg install.

## License

This project is offered under an [MIT License](LICENSE.md).
